git_url("git@github.com:BootlegCash/after-hours-ios-signing.git")
storage_mode("git")

# IMPORTANT: this must match what you're running in CI (match appstore)
type("appstore")

app_identifier([
  "com.afterhoursmedia.rankedabout"
])

